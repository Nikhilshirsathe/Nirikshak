# data/
# ├── raw/        — original unprocessed datasets
# ├── processed/  — feature-engineered datasets ready for training
# └── uploads/    — files uploaded via the platform UI (auto-managed by backend)
